spring.application.name=Trace360

# PostgreSQL Database Connection (with local fallback)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/Trace360_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Saurabh@2006}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect  # Remove this line - Hibernate auto-detects

# JWT Configuration
jwt.secret=${JWT_SECRET:Trace360SuperSecretKeyForJWTSigning2024!}
jwt.expiration-ms=86400000